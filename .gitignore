# MLflow — both recreated fresh on each CI run, never commit
mlruns/
mlflow.db

# Generated splits (recreated by pipeline)
data/train.csv
data/test.csv

# Intermediate project folder (populated during notebook run)
tourism_project/

# Python / macOS
__pycache__/
*.py[cod]
*.egg-info/
.DS_Store
.idea/
*.iml

# Jupyter checkpoints
.ipynb_checkpoints/
